using Nop.Plugin.Misc.Nexport.Domain;

namespace Nop.Plugin.Misc.Nexport.Data
{
    //public class NexportRequiredSupplementalInfoMap : NopEntityTypeConfiguration<NexportRequiredSupplementalInfo>
    //{
    //    public override void Configure(EntityTypeBuilder<NexportRequiredSupplementalInfo> builder)
    //    {
    //        builder.ToTable(nameof(NexportRequiredSupplementalInfo));

    //        builder.HasKey(m => m.Id);

    //        builder.Property(m => m.CustomerId);
    //        builder.Property(m => m.StoreId);
    //        builder.Property(m => m.QuestionId);
    //        builder.Property(m => m.UtcDateCreated);

    //        base.Configure(builder);
    //    }
    //}
}
