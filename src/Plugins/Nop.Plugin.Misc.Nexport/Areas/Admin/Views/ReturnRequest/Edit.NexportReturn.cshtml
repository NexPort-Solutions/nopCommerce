@using Nop.Core.Domain.Orders
@model Nop.Plugin.Misc.Nexport.Areas.Admin.Models.ReturnRequest.NexportReturnRequestModel

@{
    //page title
    ViewBag.PageTitle = T("Admin.ReturnRequests.EditReturnRequestDetails").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("Return requests");
}

<form asp-controller="NexportReturnRequest" asp-action="Edit" method="post">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.ReturnRequests.EditReturnRequestDetails")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.ReturnRequests.BackToList")</a>
            </small>
        </h1>
        <div class="float-right">
            <button type="submit" name="accept" class="btn btn-primary" @if(Model.ReturnRequestStatusId != (int)ReturnRequestStatus.Pending) { <text>disabled</text> }>
                <i class="fas fa-check"></i>
                Accept
            </button>
            <button type="submit" name="deny" class="btn btn-warning" @if(Model.ReturnRequestStatusId != (int)ReturnRequestStatus.Pending) { <text>disabled</text> }>
                <i class="fas fa-times"></i>
                Deny
            </button>
            <span id="returnrequest-delete" class="btn btn-danger">
                <i class="far fa-trash-alt"></i>
                @T("Admin.Common.Delete")
            </span>
            @await Component.InvokeAsync(typeof(AdminWidgetViewComponent), new { widgetZone = AdminWidgetZones.ReturnRequestDetailsButtons, additionalData = Model })
        </div>
    </div>
    @await Html.PartialAsync("~/Plugins/Misc.Nexport/Areas/Admin/Views/ReturnRequest/_CreateOrUpdate.NexportReturn.cshtml", Model)
</form>
<nop-delete-confirmation asp-model-id="@Model.Id" asp-button-id="returnrequest-delete" />